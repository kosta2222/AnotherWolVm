# Security Modifer
(PRIVATE,PUBLIC)=range(2)

# class type
(
 DEFAULT,
 STATIC,
 STRUCT,
 ENUM,
 ABSTRACT)=range(5)

(load_bytecode,load_name,invoke_function,make_function,iconstNumArgs,iload,istore,\
 return_,loads,end_file_startMain,iconst,\
iadd,isub,imul,idiv,irem,ipow,\
ieq,br,brf,\
nop)=range(21)
